class MyLocation {
  double latitude;
  double longitude;
  String storeNo;
  String storeName;
  String address;
  String brandName;

  MyLocation(this.latitude, this.longitude, this.storeNo, this.storeName,
      this.address, this.brandName);
}
